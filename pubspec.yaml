name: sobes
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  gap: ^3.0.1
  google_generative_ai: ^0.4.7
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  flutter_dotenv: ^6.0.0
  http: ^1.6.0
  dio: ^5.9.2
  speech_to_text: ^7.3.0
  permission_handler: ^12.0.1
  flutter_tts: ^4.2.5
  # 👇 ДОБАВИЛИ ЗАЩИЩЕННОЕ ХРАНИЛИЩЕ 👇
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env